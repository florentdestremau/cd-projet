{% extends 'base.html.twig' %}
{% block title %}{{ client.displayName }} — Modifier{% endblock %}
{% block body %}
    <div class="page__header">
        <div>
            <p class="text-xs text-muted" style="letter-spacing: 0.16em; text-transform: uppercase;">Modifier</p>
            <h1 class="page__title">{{ client.displayName }}</h1>
        </div>
    </div>
    {{ include('client/_form.html.twig', {form: form, action: path('app_clients_edit', {id: client.id})}) }}
{% endblock %}
